<UserControl x:Class="IDESK.Widgets.Habit.HabitGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="HabitCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Width="28" Height="28" Background="Transparent">
                            <Ellipse x:Name="Dot" Width="8" Height="8"
                                     Fill="{DynamicResource AccentBrush}"
                                     HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <Grid x:Name="CheckedContent" Visibility="Collapsed"
                                  HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Ellipse Width="20" Height="20" Fill="{DynamicResource AccentBrush}"/>
                                <Path Data="M 5,10 L 8,13 L 15,6"
                                      Stroke="White" StrokeThickness="2.5"
                                      StrokeEndLineCap="Round" StrokeStartLineCap="Round"/>
                            </Grid>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Dot" Property="Visibility" Value="Collapsed"/>
                                <Setter TargetName="CheckedContent" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 星期导航 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 0 0 10">
            <Button Width="28" Height="28" Content="‹" FontSize="16"
                    Style="{DynamicResource BorderlessButtonStyle}" Click="OnPrevWeek"/>
            <Label Content="{Binding WeekLabel}" FontSize="14" FontWeight="SemiBold"
                   VerticalAlignment="Center" Margin="10 0"/>
            <Button Width="28" Height="28" Content="›" FontSize="16"
                    Style="{DynamicResource BorderlessButtonStyle}" Click="OnNextWeek"/>
        </StackPanel>

        <!-- 表格区域 -->
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <ScrollViewer.Resources>
                <Style TargetType="ScrollBar" BasedOn="{StaticResource ThinScrollBarStyle}"/>
            </ScrollViewer.Resources>
            <ContentControl x:Name="TableContainer"/>
        </ScrollViewer>
    </Grid>
</UserControl>
