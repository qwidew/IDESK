<UserControl x:Class="IDESK.Widgets.Schedule.TodayScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="今日日程" FontSize="15" FontWeight="Bold"
               HorizontalAlignment="Center" Margin="0 0 0 8"/>
        <ItemsControl x:Name="ItemList" Grid.Row="1"
                      BorderThickness="0" Background="Transparent"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="4" Background="{DynamicResource ItemBgBrush}" Padding="8 5" Margin="0 2">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Content="{Binding Time}" FontSize="12"
                                   Foreground="{DynamicResource TextSecondaryBrush}" Padding="0" Margin="0 8 0 0"/>
                            <TextBlock Grid.Column="1" Text="{Binding Content}" FontSize="13"
                                       VerticalAlignment="Center" TextWrapping="Wrap" Margin="8 0 0 0"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
