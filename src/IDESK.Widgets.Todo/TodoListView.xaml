<UserControl x:Class="IDESK.Widgets.Todo.TodoListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:IDESK.Widgets.Todo.Control"
        mc:Ignorable="d"
        d:DesignHeight="700" d:DesignWidth="350">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" HorizontalAlignment="Center" Margin="0 10 0 0">
            <Label x:Name="GroupNameText" Content="{Binding GroupName}" FontSize="16"
                   Padding="0" VerticalAlignment="Center"
                   PreviewMouseLeftButtonDown="OnEditGroupName"/>
            <TextBox x:Name="GroupNameBox" Visibility="Collapsed"
                     Text="{Binding GroupName, UpdateSourceTrigger=LostFocus}"
                     LostFocus="OnEditGroupEnd" KeyDown="OnEditGroupKeyDown"
                     Style="{DynamicResource InlineEditBoxStyle}"
                     FontSize="16" HorizontalAlignment="Center"/>
        </Grid>

        <Grid Grid.Row="1" Margin="0 5 0 0">
            <TextBox x:Name="TxtBoxTodo" Width="160" Height="21"
                     HorizontalAlignment="Center"
                     Style="{DynamicResource DefaultTextBoxStyle}"
                     Text="{Binding NewContent, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Width="24" Height="21" HorizontalAlignment="Right" Margin="0 0 8 0"
                    Style="{DynamicResource BorderlessButtonStyle}"
                    Command="{Binding SortCommand}" ToolTip="按截止日期排序">
                <Path Data="{DynamicResource IconSort}" Stretch="Uniform"
                      Fill="{DynamicResource ActionIconBrush}" Width="12" Height="12"/>
            </Button>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5"
                    HorizontalAlignment="Center">
            <Button Width="85" Margin="5 0"
                    Style="{DynamicResource DefaultButtonStyle}"
                    Height="25" Command="{Binding AddCommand}">
                <StackPanel Orientation="Horizontal">
                    <Path Data="{DynamicResource IconAdd}" Stretch="Uniform" Fill="{DynamicResource TextPrimaryBrush}"
                          Width="10" Height="10"/>
                    <Label Content="添加代办" FontSize="11" Foreground="{DynamicResource TextPrimaryBrush}"/>
                </StackPanel>
            </Button>
            <Button Width="90" Margin="5 0"
                    Style="{DynamicResource DangerButtonStyle}"
                    Height="25" Command="{Binding ClearCommand}">
                <StackPanel Orientation="Horizontal">
                    <Path Data="{DynamicResource IconDelete}" Stretch="Uniform" Fill="{DynamicResource DangerTextBrush}"
                          Width="12" Height="12"/>
                    <Label Content="删除完成" FontSize="11" Foreground="{DynamicResource DangerTextBrush}"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <Button Grid.Row="2" Width="75" Height="25" HorizontalAlignment="Right" Margin="40 5 5 5"
                Style="{DynamicResource BorderlessButtonStyle}"
                Command="{Binding ToggleFilterCommand}">
            <StackPanel Orientation="Horizontal">
                <Grid Width="12" Height="12">
                    <Path Data="{DynamicResource IconHide}" Stretch="Uniform" Fill="{DynamicResource TextPrimaryBrush}">
                        <Path.Style>
                            <Style TargetType="Path">
                                <Setter Property="Opacity" Value="1"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ShowCompleted}" Value="False">
                                        <Setter Property="Opacity" Value="0"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Path.Style>
                    </Path>
                    <Path Data="{DynamicResource IconVisibility}" Stretch="Uniform" Fill="{DynamicResource TextPrimaryBrush}">
                        <Path.Style>
                            <Style TargetType="Path">
                                <Setter Property="Opacity" Value="0"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ShowCompleted}" Value="False">
                                        <Setter Property="Opacity" Value="1"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Path.Style>
                    </Path>
                </Grid>
                <Label FontSize="10" Foreground="{DynamicResource TextPrimaryBrush}">
                    <Label.Style>
                        <Style TargetType="Label">
                            <Setter Property="Content" Value="隐藏完成"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ShowCompleted}" Value="False">
                                    <Setter Property="Content" Value="显示全部"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Label.Style>
                </Label>
            </StackPanel>
        </Button>

        <ProgressBar Grid.Row="3" />

        <Grid Grid.Row="4" Margin="20 5">
            <ListBox ItemsSource="{Binding FilteredView}"
                     AllowDrop="True"
                     VirtualizingStackPanel.IsVirtualizing="False"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     PreviewMouseLeftButtonDown="OnPreviewMouseLeftButtonDown"
                     DragOver="OnDragOver" Drop="OnDrop" DragLeave="OnDragLeave"
                     GiveFeedback="OnGiveFeedback">
                <ListBox.Resources>
                    <Style TargetType="ScrollBar" BasedOn="{StaticResource ThinScrollBarStyle}"/>
                </ListBox.Resources>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <controls:TodoItemBlock TodoContent="{Binding Content}"
                                                IsDone="{Binding IsDone}"
                                                CompleteCommand="{Binding DataContext.CompleteItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                DeleteCommand="{Binding DataContext.DeleteItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Canvas IsHitTestVisible="False">
                <Rectangle x:Name="InsertionLine" Fill="{DynamicResource AccentBrush}"
                           Height="2" Visibility="Collapsed"/>
            </Canvas>
        </Grid>
    </Grid>
</UserControl>
